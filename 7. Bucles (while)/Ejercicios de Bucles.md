# 🔁 Ejercicios — `while` en Python

Practicá el uso de `while` con estos ejercicios progresivos.

---

## Ejercicio 1 — Números del 1 al 10

**Enunciado:** Mostrá en pantalla todos los números del `1` al `10`, uno por línea.

**Debe usar:** `while`, una variable contadora, `print()`

**Pista:** Inicializá la variable en `1` y aumentala en `1` en cada vuelta del bucle. El bucle se repite mientras la variable sea `<= 10`.

```python
# Tu código aquí
```

---

## Ejercicio 2 — Números del 10 al 1

**Enunciado:** Mostrá en pantalla los números del `10` al `1` en orden descendente, uno por línea.

**Debe usar:** `while`, una variable contadora, `print()`

**Pista:** Inicializá la variable en `10` y restale `1` en cada vuelta. El bucle se repite mientras la variable sea `>= 1`.

```python
# Tu código aquí
```

---

## Ejercicio 3 — Números pares del 2 al 20

**Enunciado:** Mostrá solo los números pares entre `2` y `20`, uno por línea.

**Debe usar:** `while`, una variable contadora, `print()`

**Pista:** Iniciá la variable en `2` y sumale `2` en cada vuelta para saltar directamente al siguiente par.

```python
# Tu código aquí
```

---

## Ejercicio 4 — Números impares del 1 al 15

**Enunciado:** Mostrá solo los números impares entre `1` y `15`, uno por línea.

**Debe usar:** `while`, una variable contadora, `print()`

**Pista:** Iniciá la variable en `1` y sumale `2` en cada vuelta para saltar de impar en impar.

```python
# Tu código aquí
```

---

## Ejercicio 5 — Contar hasta un número

**Enunciado:** Pedile al usuario un número y mostrá todos los números del `1` hasta ese número.

**Debe usar:** `while`, `input()`, `int()`, una variable contadora, `print()`

**Pista:** Guardá el número del usuario en una variable y usala como límite en la condición del `while`.

```python
# Tu código aquí
```

---

## Ejercicio 6 — Números hasta el cero

**Enunciado:** Pedí números al usuario uno por uno. Cuando ingrese `0`, el programa debe detenerse.

**Debe usar:** `while`, `input()`, `int()`, `if`, `break` (o condición en el `while`)

**Pista:** Podés guardar el primer número antes del bucle y actualizar la variable al final de cada vuelta.

```python
# Tu código aquí
```

---

## Ejercicio 7 — Suma hasta el cero

**Enunciado:** Pedí números al usuario y sumalos. Cuando ingrese `0`, mostrá el total acumulado.

**Debe usar:** `while`, `input()`, `int()`, una variable acumuladora, `print()`

**Pista:** Inicializá `total = 0` antes del bucle y sumale cada número ingresado dentro del bucle.

```python
# Tu código aquí
```

---

## Ejercicio 8 — Contar cuántos números ingresa el usuario

**Enunciado:** Pedí números al usuario. Cuando ingrese `0`, mostrá cuántos números escribió en total (sin contar el `0`).

**Debe usar:** `while`, `input()`, `int()`, un contador, `print()`

**Pista:** Usá una variable `contador = 0` que sume `1` cada vez que el usuario ingrese un número distinto de `0`.

```python
# Tu código aquí
```

---

## Ejercicio 9 — Tabla del 3

**Enunciado:** Mostrá la tabla de multiplicar del `3` del `1` al `10` usando un bucle `while`.

**Debe usar:** `while`, una variable contadora, `print()`

**Pista:** En cada vuelta multiplicá el contador por `3` y mostrá el resultado con un formato tipo `3 x 1 = 3`.

```python
# Tu código aquí
```

---

## Ejercicio 10 — Sistema de contraseña

**Enunciado:** Pedí una contraseña al usuario repetidamente hasta que ingrese la correcta (`"1234"`). Al lograrlo, mostrá un mensaje de acceso concedido.

**Debe usar:** `while`, `input()`, `if`, `break`, `print()`

**Pista:** Guardá la contraseña correcta en una variable antes del bucle y comparala con cada intento del usuario.

```python
# Tu código aquí
```

---

> 💡 **Recordá:** Todo `while` necesita que su condición cambie en algún momento, o el bucle nunca terminará.
