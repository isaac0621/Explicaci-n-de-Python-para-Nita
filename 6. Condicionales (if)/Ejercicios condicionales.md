# ✅ Ejercicios — Condicionales en Python

Practicá el uso de `if`, `elif` y `else` con estos ejercicios.

---

## Ejercicio 1 — Mayor de edad

**Enunciado:** Pedile al usuario su edad y mostrá un mensaje indicando si es mayor o menor de edad.

**Debe usar:** `input()`, `int()`, `if`, `else`, `print()`

**Pista:** Una persona es mayor de edad si su `edad >= 18`.

```python
# Tu código aquí
```

---

## Ejercicio 2 — Número mayor a 10

**Enunciado:** Pedile al usuario un número y mostrá si es mayor, menor o igual a `10`.

**Debe usar:** `input()`, `int()`, `if`, `elif`, `else`, `print()`

**Pista:** Necesitás tres casos: mayor, menor e igual. Usá `if`, `elif` y `else`.

```python
# Tu código aquí
```

---

## Ejercicio 3 — Número positivo

**Enunciado:** Pedile al usuario un número y mostrá si es positivo, negativo o cero.

**Debe usar:** `input()`, `int()`, `if`, `elif`, `else`, `print()`

**Pista:** Un número es positivo si es `> 0`, negativo si es `< 0`, y cero si es igual a `0`.

```python
# Tu código aquí
```

---

## Ejercicio 4 — Validar contraseña

**Enunciado:** Pedile al usuario una contraseña y mostrá si es correcta o no. La contraseña correcta es `"python"`.

**Debe usar:** `input()`, `if`, `else`, `print()`

**Pista:** Comparás el valor ingresado con el string `"python"` usando `==`.

```python
# Tu código aquí
```

---

## Ejercicio 5 — Verificar nota

**Enunciado:** Pedile al usuario una nota y mostrá si aprobó (nota `>= 70`) o reprobó.

**Debe usar:** `input()`, `int()`, `if`, `else`, `print()`

**Pista:** La condición de aprobado es `nota >= 70`.

```python
# Tu código aquí
```

---

## Ejercicio 6 — Comparar dos números

**Enunciado:** Pedile al usuario dos números y mostrá si el primero es mayor, menor o igual al segundo.

**Debe usar:** dos `input()`, dos `int()`, `if`, `elif`, `else`, `print()`

**Pista:** Comparás `numero1` con `numero2` usando `>`, `<` y `==`.

```python
# Tu código aquí
```

---

## Ejercicio 7 — Mostrar el mayor

**Enunciado:** Pedile al usuario dos números y mostrá cuál de los dos es el mayor. Si son iguales, indicalo.

**Debe usar:** dos `input()`, dos `int()`, `if`, `elif`, `else`, `print()`

**Pista:** Usá `if numero1 > numero2` para el primer caso, `elif numero2 > numero1` para el segundo, y `else` para cuando son iguales.

```python
# Tu código aquí
```

---

## Ejercicio 8 — Validar edad

**Enunciado:** Pedile al usuario su edad. Si es menor a `0` o mayor a `120`, mostrá que es inválida. Si no, mostrala.

**Debe usar:** `input()`, `int()`, `if`, `else`, `print()`

**Pista:** Una edad válida está entre `0` y `120`. Podés combinar condiciones con `or`.

```python
# Tu código aquí
```

---

## Ejercicio 9 — Mensaje según condición

**Enunciado:** Pedile al usuario su temperatura corporal y mostrá si tiene fiebre (`>= 38`), está normal (`>= 36`) o tiene hipotermia (menos de `36`).

**Debe usar:** `input()`, `float()`, `if`, `elif`, `else`, `print()`

**Pista:** Organizá los casos de mayor a menor para que las condiciones funcionen correctamente.

```python
# Tu código aquí
```

---

## Ejercicio 10 — Validar compra

**Enunciado:** Pedile al usuario el monto de una compra. Si supera `5000`, mostrá que tiene descuento del 10%. Si no, mostrá el precio sin descuento.

**Debe usar:** `input()`, `float()`, `if`, `else`, `print()`

**Pista:** El precio con descuento se calcula como `monto * 0.9` (se quita el 10%).

```python
# Tu código aquí
```

---

> 💡 **Recordá:** `if` evalúa la primera condición, `elif` evalúa condiciones adicionales, y `else` se ejecuta si ninguna condición anterior fue verdadera.
