# 🗂️ Ejercicios — Tipos de Datos en Python

Practicá el uso de los tipos de datos básicos: `str`, `int` y `float`.

---

## Ejercicio 1 — Crear string

**Enunciado:** Creá una variable de tipo `str` (texto) y asignale cualquier palabra o frase.

**Debe usar:** una variable, un valor entre comillas

**Pista:** Todo lo que va entre comillas es un `str`, sin importar qué contenga.

```python
# Tu código aquí
```

---

## Ejercicio 2 — Crear int

**Enunciado:** Creá una variable de tipo `int` (número entero) y asignale cualquier número sin decimales.

**Debe usar:** una variable, un número entero

**Pista:** Los enteros no llevan comillas ni punto decimal: `numero = 42`.

```python
# Tu código aquí
```

---

## Ejercicio 3 — Crear float

**Enunciado:** Creá una variable de tipo `float` (número decimal) y asignale cualquier número con punto decimal.

**Debe usar:** una variable, un número con punto decimal

**Pista:** En Python los decimales usan punto, no coma: `precio = 9.99`.

```python
# Tu código aquí
```

---

## Ejercicio 4 — Mostrar string

**Enunciado:** Creá una variable de tipo `str` y mostrala en pantalla.

**Debe usar:** una variable string, `print()`

**Pista:** Pasá la variable directamente a `print()` sin comillas adicionales.

```python
# Tu código aquí
```

---

## Ejercicio 5 — Mostrar int

**Enunciado:** Creá una variable de tipo `int` y mostrala en pantalla.

**Debe usar:** una variable entera, `print()`

**Pista:** `print()` puede mostrar números igual que texto, sin ningún cambio extra.

```python
# Tu código aquí
```

---

## Ejercicio 6 — Mostrar float

**Enunciado:** Creá una variable de tipo `float` y mostrala en pantalla.

**Debe usar:** una variable decimal, `print()`

**Pista:** El resultado mostrará el número con su punto decimal tal como lo guardaste.

```python
# Tu código aquí
```

---

## Ejercicio 7 — Tres tipos distintos

**Enunciado:** Creá tres variables, una de cada tipo: `str`, `int` y `float`.

**Debe usar:** tres variables con tipos distintos

**Pista:** Por ejemplo: un nombre (str), una cantidad (int) y un precio (float).

```python
# Tu código aquí
```

---

## Ejercicio 8 — Mostrar todos

**Enunciado:** Tomá las tres variables del ejercicio anterior y mostralas todas en pantalla.

**Debe usar:** tres variables, tres `print()`

**Pista:** Usá un `print()` por variable para que cada valor aparezca en su propia línea.

```python
# Tu código aquí
```

---

## Ejercicio 9 — Variable decimal

**Enunciado:** Creá una variable llamada `temperatura` con un valor decimal y mostrala.

**Debe usar:** una variable `float`, `print()`

**Pista:** Usá un valor como `36.5` o `22.3` y mostralo con un mensaje descriptivo.

```python
# Tu código aquí
```

---

## Ejercicio 10 — Variable texto

**Enunciado:** Creá una variable llamada `mensaje` con una frase completa y mostrala en pantalla.

**Debe usar:** una variable `str`, `print()`

**Pista:** Podés escribir una oración dentro de las comillas: `mensaje = "Python es genial"`.

```python
# Tu código aquí
```

---

> 💡 **Recordá:** Podés verificar el tipo de una variable con `type(variable)`. Por ejemplo, `print(type(edad))` te dirá `<class 'int'>`.
