# 🔄 Ejercicios — Conversión de Tipos en Python

Practicá cómo convertir valores entre distintos tipos de datos usando `int()`, `float()` y `str()`.

---

## Ejercicio 1 — Convertir edad a int

**Enunciado:** Pedile al usuario su edad con `input()` y convertila a número entero.

**Debe usar:** `input()`, `int()`, una variable

**Pista:** `input()` siempre devuelve un string. Envolvelo con `int()` para convertirlo: `int(input(...))`.

```python
# Tu código aquí
```

---

## Ejercicio 2 — Sumar 5 a la edad

**Enunciado:** Pedile al usuario su edad, convertila a entero y mostrá cuántos años tendrá en 5 años.

**Debe usar:** `input()`, `int()`, suma, `print()`

**Pista:** Una vez convertida a `int`, podés operarla como cualquier número: `edad + 5`.

```python
# Tu código aquí
```

---

## Ejercicio 3 — Convertir decimal

**Enunciado:** Pedile al usuario un número decimal y convertilo a `float`.

**Debe usar:** `input()`, `float()`, una variable

**Pista:** Usá `float(input(...))` para convertir directamente el valor ingresado.

```python
# Tu código aquí
```

---

## Ejercicio 4 — Multiplicar número

**Enunciado:** Pedile al usuario un número entero, convertilo y multiplicalo por `3`. Mostrá el resultado.

**Debe usar:** `input()`, `int()`, multiplicación, `print()`

**Pista:** Guardá el número ya convertido en una variable antes de multiplicarlo.

```python
# Tu código aquí
```

---

## Ejercicio 5 — Sumar dos números convertidos

**Enunciado:** Pedile al usuario dos números por separado, convertí ambos a enteros y mostrá su suma.

**Debe usar:** dos `input()`, dos `int()`, suma, `print()`

**Pista:** Convertí cada número al guardarlo en su variable. Luego sumá las dos variables.

```python
# Tu código aquí
```

---

## Ejercicio 6 — Restar números

**Enunciado:** Pedile al usuario dos números, convertí ambos y mostrá la resta del primero menos el segundo.

**Debe usar:** dos `input()`, dos `int()`, resta, `print()`

**Pista:** La resta es `numero1 - numero2`. Asegurate de convertir ambos antes de operar.

```python
# Tu código aquí
```

---

## Ejercicio 7 — Convertir texto a número

**Enunciado:** Pedile al usuario que ingrese un número escrito como texto (por ejemplo `"7"`) y convertilo a `int` para usarlo en una operación.

**Debe usar:** `input()`, `int()`, cualquier operación matemática, `print()`

**Pista:** Lo que el usuario escribe siempre llega como string. `int()` lo transforma en número operable.

```python
# Tu código aquí
```

---

## Ejercicio 8 — Mostrar resultado

**Enunciado:** Pedí un número al usuario, convertilo a entero, calculá su doble y mostrá el resultado con un mensaje claro.

**Debe usar:** `input()`, `int()`, multiplicación, `print()`

**Pista:** El doble de un número es `numero * 2`. Mostralo con: `print("El doble es:", resultado)`.

```python
# Tu código aquí
```

---

## Ejercicio 9 — Convertir float

**Enunciado:** Pedile al usuario un número decimal, convertilo a `float` y mostrá el resultado de multiplicarlo por `2`.

**Debe usar:** `input()`, `float()`, multiplicación, `print()`

**Pista:** Usá `float()` en lugar de `int()` para aceptar números con punto decimal.

```python
# Tu código aquí
```

---

## Ejercicio 10 — Multiplicar valores

**Enunciado:** Pedile al usuario dos números decimales, convertí ambos a `float` y mostrá su producto.

**Debe usar:** dos `input()`, dos `float()`, multiplicación, `print()`

**Pista:** El producto es `numero1 * numero2`. Mostralo con un mensaje como `"El resultado es:"`.

```python
# Tu código aquí
```

---

> 💡 **Recordá:** `int()` convierte a entero, `float()` a decimal y `str()` a texto. Siempre convertí antes de operar matemáticamente con datos del usuario.
